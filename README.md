# speed-booster

Load the script directly with this one-liner:

```lua
loadstring(game:HttpGet("https://raw.githubusercontent.com/WUXOHUB/speed-booster/main/WUXO_HUB.lua"))()
```

> Note: After committing and pushing this repository to GitHub, the raw URL above will serve the file so you can use the one-liner.
